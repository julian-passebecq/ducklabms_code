-r requirements-engines.txt
# Real local dbt Project mode. The bounded exercise adapter does not need dbt Core.
# The runner supports manifest v9-v12 and run-results v4-v6; other versions fail closed.
dbt-core>=1.10,<1.12
dbt-duckdb>=1.10,<2
