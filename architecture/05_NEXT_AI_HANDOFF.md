# Continue this implemented candidate - no restart

Read `../docs/v1/RELEASE_STATUS.md`, `../docs/v1/MIGRATION_AND_PROVENANCE.md`, and `../docs/v1/LOCAL_RUN.md` first. The bundled source is complete, not an overlay. The exact original GitHub base is `abb0345a78836508f6024d79710a46973cbed9d6`; it is not the final edited candidate. The delivery manifest identifies the candidate tree and every file.

Do not start from main, fetch a newer branch, or reapply Pre-Pro/M2. Preserve the already-implemented resource migration, same-source multi-view behavior, job locks/provenance, compiler safety and explicit runtime truth.

Run the declared dependency install, full application typecheck/build, full backend suite, native smoke commands and native Playwright journeys on a machine with those dependencies. Fix evidence-backed integration failures. Do not turn the previous model/component tests into a native certification claim. The `qa/v1/final/` logs preserve actual failed/blocked gates and successful focused tests.

Resolve the functional limitations explicitly listed in the release report, including DAG-IR Arena grading and the qualified interoperability boundaries. Do not revive Oracle, Iceberg, Airflow/Prefect/Kubernetes, distributed Spark, or generic terminal/Git/Linux/SSH/IaC scope. No deployment work is needed to qualify the local product.

Return edited source, exact changed files and real gate evidence. Only declare V1 complete after the original acceptance gates in `../docs/v1/ORIGINAL_ACCEPTANCE_GATES.md` pass.
