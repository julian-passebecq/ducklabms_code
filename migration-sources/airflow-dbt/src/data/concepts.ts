export interface LearningConcept {
  id: string;
  title: string;
  product: 'Airflow' | 'dbt' | 'Hybrid';
  definition: string;
  why: string;
  realProject: string;
  example: string;
  mistake: string;
  reflection?: string;
}

export const concepts: LearningConcept[] = [
  { id:'retries', title:'Airflow retries', product:'Airflow', definition:'A task-level policy that schedules another attempt after a failed attempt.', why:'Useful for transient failures such as a temporary API or network problem.', realProject:'Configure retries only around work that can safely be repeated.', example:'retries=2, retry_delay=timedelta(minutes=5)', mistake:'Using retries to hide deterministic data-quality failures.', reflection:'Which failures are transient, and which require a code/data fix?' },
  { id:'sensors', title:'Sensors', product:'Airflow', definition:'Tasks that wait until an external condition becomes true.', why:'They express readiness dependencies that are not ordinary in-DAG task dependencies.', realProject:'Wait for a file, partition, API condition, or upstream workflow event.', example:'@task.sensor(...): return PokeReturnValue(is_done=ready)', mistake:'Polling too aggressively or waiting for a condition Airflow should not own.', reflection:'Could the dependency be event-driven instead of polled?' },
  { id:'branching', title:'Branching and skipping', product:'Airflow', definition:'A branch chooses which downstream path should run; non-selected paths are skipped.', why:'It models conditional orchestration without putting every condition inside one giant task.', realProject:'Route clean vs quarantine paths or choose a full vs incremental load.', example:'route_quality → [publish, quarantine]', mistake:'Treating skipped as the same as upstream_failed. A branch skip is deliberate; upstream_failed means a trigger rule could not be satisfied because an upstream task failed.' },
  { id:'xcom', title:'XCom', product:'Airflow', definition:'Small metadata values exchanged between task instances.', why:'Lets orchestration tasks share identifiers or tiny results.', realProject:'Pass a loaded partition name, row count, or object URI.', example:'return {"rows": 12490} → downstream task argument', mistake:'Moving large datasets through XCom instead of durable storage.' },
  { id:'catchup', title:'Catchup and backfill', product:'Airflow', definition:'Catchup creates scheduled historical runs from a DAG start date; backfill explicitly creates/runs historical intervals.', why:'Historical intervals matter when pipelines are partitioned by logical time.', realProject:'Recompute missed daily partitions after a deployment or outage.', example:'schedule="0 6 * * *", catchup=False for only current scheduled operation.', mistake:'Confusing a retry of one task with creating historical DAG runs.' },
  { id:'task-groups', title:'Task groups', product:'Airflow', definition:'A visual/logical grouping of tasks inside one DAG.', why:'Keeps large DAGs readable without creating a separate scheduler boundary.', realProject:'Group ingestion, quality, transformation, and publication tasks.', example:'with TaskGroup("quality"): ...', mistake:'Assuming a task group has its own independent schedule.' },
  { id:'trigger-rules', title:'Trigger rules', product:'Airflow', definition:'A rule that decides whether a task may run after its upstream task instances reach terminal states.', why:'Branch joins and cleanup tasks often need behavior other than the default all_success rule.', realProject:'Join clean and quarantine branches with none_failed_min_one_success so one selected path may be skipped without blocking the join.', example:'trigger_rule="none_failed_min_one_success"', mistake:'Using all_success after a branch, or putting a successful all_done task at the very end without realizing Airflow evaluates DagRun terminal state from leaf tasks.' },
  { id:'ref', title:'dbt ref()', product:'dbt', definition:'Declares a dependency on another dbt resource and resolves its relation name.', why:'The dependency becomes part of the dbt DAG, enabling ordered builds and lineage.', realProject:'A mart references an intermediate model instead of hard-coding its warehouse table name.', example:"from {{ ref('int_customer_orders') }}", mistake:'Hard-coding analytics schema names and losing dependency metadata.' },
  { id:'source', title:'dbt source()', product:'dbt', definition:'References an externally loaded relation declared as a dbt source.', why:'Separates upstream/raw ownership from models dbt itself builds.', realProject:'Declare raw.orders with freshness/description metadata, then reference it from staging.', example:"from {{ source('raw', 'orders') }}", mistake:'Treating every raw table as if dbt created it.' },
  { id:'materializations', title:'Materializations', product:'dbt', definition:'The strategy used to persist a model: view, table, incremental, or ephemeral.', why:'Different models have different freshness, cost, reuse, and performance needs.', realProject:'Staging often uses views; heavy marts may be tables/incremental; tiny helpers may be ephemeral.', example:"{{ config(materialized='incremental', unique_key='order_id') }}", mistake:'Choosing incremental before understanding update semantics and late-arriving data.' },
  { id:'ephemeral', title:'Ephemeral models', product:'dbt', definition:'Models compiled into dependent SQL as CTEs instead of becoming physical warehouse relations.', why:'Useful for small reusable transformations that do not merit persisted relations.', realProject:'A narrow helper transformation reused by one or two downstream models.', example:"{{ config(materialized='ephemeral') }}", mistake:'Building long chains of ephemeral models that make compiled SQL hard to debug.' },
  { id:'incremental', title:'Incremental models', product:'dbt', definition:'Models that process only a subset of rows after the first/full build.', why:'Reduce compute for large fact-like models when change boundaries are reliable.', realProject:'Reprocess rows where event_time is newer than the maximum already stored, plus a late-data lookback.', example:'{% if is_incremental() %} where event_time >= ... {% endif %}', mistake:'Ignoring updates, duplicates, or late-arriving records.' },
  { id:'tests', title:'dbt data tests', product:'dbt', definition:'Queries/assertions that return failing records when an expectation is violated.', why:'Turn important data assumptions into executable quality gates.', realProject:'unique + not_null order_id; relationships customer_id → customers.', example:'unique, not_null, relationships, accepted_values, plus singular tests.', mistake:'Testing everything equally instead of prioritizing business-critical contracts.' },
  { id:'jinja', title:'Macros and Jinja', product:'dbt', definition:'Templating constructs evaluated while dbt compiles model code into warehouse SQL.', why:'Enables reusable SQL generation and environment-aware behavior.', realProject:'Standardize currency conversion, surrogate keys, or audit columns.', example:"{{ cents_to_currency('amount_cents') }}", mistake:'Writing complex application logic in templates that obscures the resulting SQL.' },
  { id:'seeds', title:'Seeds', product:'dbt', definition:'Small version-controlled CSV datasets that dbt can load into the warehouse.', why:'Useful for relatively static lookup/reference data maintained with the project.', realProject:'Country codes, small mappings, or controlled classifications.', example:'seeds/country_codes.csv → dbt seed', mistake:'Using seeds for large or frequently changing operational datasets.' },
  { id:'docs', title:'dbt documentation', product:'dbt', definition:'Descriptions and metadata attached to sources, models, columns, tests and lineage.', why:'Makes transformation logic discoverable instead of relying on tribal knowledge.', realProject:'A mart explains grain, ownership, freshness expectations and column meaning.', example:'description: Daily customer revenue net of returns', mistake:'Documenting only file names without explaining model grain or business meaning.' },
  { id:'snapshots', title:'Snapshots', product:'dbt', definition:'A dbt resource that records changes to mutable source rows over time.', why:'Useful when upstream systems overwrite current state but analytics needs history.', realProject:'Track customer tier or account status history.', example:'timestamp/check strategy over a business key.', mistake:'Using snapshots when the source already provides immutable history.' },
  { id:'contracts', title:'Model contracts', product:'dbt', definition:'Declared output column names/types that dbt can enforce for supported materializations/adapters.', why:'Makes a model interface explicit for downstream consumers.', realProject:'A published finance mart with stable typed columns.', example:'contract.enforced: true with declared columns.', mistake:'Treating contracts as a replacement for business-value data tests.' },
  { id:'orchestration', title:'Airflow + dbt boundary', product:'Hybrid', definition:'Airflow decides when/under what external conditions dbt runs; dbt decides SQL model dependency order inside the transformation project.', why:'Each tool owns the layer it models well.', realProject:'Airflow waits for ingestion, triggers selected dbt models/tests, then publishes downstream.', example:'load_raw → run_dbt_marts → dbt_test → publish', mistake:'Duplicating every dbt model as a separately hand-wired Airflow task without a reason.' },
];

export const findConcept = (id: string) => concepts.find((concept) => concept.id === id);
