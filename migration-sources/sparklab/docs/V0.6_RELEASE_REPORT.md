# V0.6 Release Report — Fabric-inspired Notebook Workbench

## Scope completed

- Replaced the V0.5 tutorial-first shell with a notebook-first workbench.
- Added Fabric-inspired black app bar and compact ribbon/command bar.
- Added runtime selector: PySpark Training, SQL (DuckDB), Polars, MotherDuck SQL.
- Added virtual environment selector: Fabric-like F64, Databricks-like Jobs, Local 8x8.
- Added attached DuckLake selector and Explorer data tree.
- Added table/field/file browsing for all four core case studies.
- Added editable notebook code cell and runtime-specific code templates.
- Added Markdown mission cell and result preview.
- Added persistent real-vs-simulated truth labels.
- Added right diagnostics pane with Spark UI, Lakehouse, Quality, Cost and Hints.
- Preserved Overview, Pipeline, Lakehouse, Consumer and Mission workspaces behind the global rail.
- Preserved V0.5 FastAPI/SparkLab runtime code and truth-pack tests.

## Regression/correctness checks

### SparkLab

`services/sparklab/test_sparklab.py` + `services/sparklab/tests/test_v05_runtime.py`

- **8 passed**

### FastAPI

`services/api/test_api.py`

- **4 passed**

### React/TypeScript source

Validated with TypeScript 5.8 in strict/no-emit mode using local React declaration stubs because npm registry access is unavailable in this environment.

- **PASS**

### Standalone JavaScript

Extracted the inline JS from `standalone.html` and validated with `node --check`.

- **PASS**

## Known environment limitation

The npm registry is unreachable from the current execution environment, so a fresh `npm install` / Vite production build could not be performed. `standalone.html` is intentionally dependency-free and is the immediately testable artifact.

Headless Chromium is installed but does not complete screenshot rendering in this container because of its DBus/zygote environment. Therefore no successful browser screenshot gate is claimed for this pass.
