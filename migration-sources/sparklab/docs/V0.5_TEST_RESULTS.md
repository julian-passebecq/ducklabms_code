# V0.5 test results

Executed 2026-09-17.

## Automated runtime/API suite

`12 passed`

Coverage includes:

- safe Retail PySpark AST compilation;
- unsafe arbitrary import rejection;
- Mobility window compilation;
- Energy bounded-window compilation;
- broadcast vs SortMerge shuffle regression;
- AQE skew-split task-shape regression;
- memory-pressure/spill regression;
- SparkLab cost adapter regression;
- shared-capacity queueing;
- FastAPI health/simulate/grade endpoints;
- alternative valid-solution grading;
- anti-pattern grading.

## Compatibility checks

- legacy SparkLab compiler/window/estimate tests: PASS;
- Python `py_compile`: PASS;
- standalone JavaScript `node --check`: PASS;
- strict TypeScript source check with temporary React stubs: PASS;
- standalone content markers: PASS.

## Rendering limitation

The container's headless Chromium process did not exit cleanly in this environment, so browser screenshot QA was not used as a release gate. The dependency-free standalone is included for direct browser review.
