# Optional only. The default app uses a deterministic local learning backend.
# For a personal browser-only MotherDuck session, paste a token in the app's Data Engine dialog.
# Do not commit real tokens.
